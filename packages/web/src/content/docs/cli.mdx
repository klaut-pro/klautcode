---
title: CLI
description: Klautcode CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Klautcode CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
klautcode
```

But it also accepts commands as documented on this page. This allows you to interact with Klautcode programmatically.

```bash
klautcode run "Explain how closures work in JavaScript"
```

---

### tui

Start the Klautcode terminal user interface.

```bash
klautcode [project]
```

#### Flags

| Flag                                        | Short | Description                                                             |
| ------------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr>    | `-c`  | Continue the last session                                               |
| <nobr><code>{"--session"}</code></nobr>     | `-s`  | Session ID to continue                                                  |
| <nobr><code>{"--fork"}</code></nobr>        |       | Fork the session when continuing (use with `--continue` or `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>      |       | Prompt to use                                                           |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use in the form of provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>       |       | Agent to use                                                            |
| <nobr><code>{"--auto"}</code></nobr>        |       | Auto-approve permissions that are not explicitly denied                 |
| <nobr><code>{"--port"}</code></nobr>        |       | Port to listen on                                                       |
| <nobr><code>{"--hostname"}</code></nobr>    |       | Hostname to listen on                                                   |
| <nobr><code>{"--mdns"}</code></nobr>        |       | Enable mDNS discovery                                                   |
| <nobr><code>{"--mdns-domain"}</code></nobr> |       | Custom mDNS domain name                                                 |
| <nobr><code>{"--cors"}</code></nobr>        |       | Additional browser origin(s) to allow CORS                              |

---

## Commands

The Klautcode CLI also has the following commands.

---

### agent

Manage agents for Klautcode.

```bash
klautcode agent [command]
```

---

#### create

Create a new agent with custom configuration.

```bash
klautcode agent create
```

This command will guide you through creating a new agent with a custom system prompt and permission configuration. Anything you don't allow is denied in the generated agent's frontmatter.

#### Flags

| Flag                                        | Short | Description                                                                                                                                                                                                                |
| ------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--path"}</code></nobr>        |       | Directory to write the agent file to (defaults to global or `.klautcode/agent` based on the prompt)                                                                                                                         |
| <nobr><code>{"--description"}</code></nobr> |       | What the agent should do                                                                                                                                                                                                   |
| <nobr><code>{"--mode"}</code></nobr>        |       | Agent mode: `all`, `primary`, or `subagent`                                                                                                                                                                                |
| <nobr><code>{"--permissions"}</code></nobr> |       | Comma-separated list of permissions to allow (default: all). Available: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Anything omitted is denied. Alias: `--tools` |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use, in `provider/model` format                                                                                                                                                                                   |

Passing all of `--path`, `--description`, `--mode`, and `--permissions` runs the command non-interactively.

---

#### list

List all available agents.

```bash
klautcode agent list
```

---

### attach

Attach a terminal to an already running Klautcode backend server started via `serve` or `web` commands.

```bash
klautcode attach [url]
```

This allows using the TUI with a remote Klautcode backend. For example:

```bash
# Start the backend server for web/mobile access
klautcode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
klautcode attach http://10.20.30.40:4096
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory to start TUI in                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `KLAUTCODE_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `KLAUTCODE_SERVER_USERNAME` or `klautcode`) |

---

### auth

Command to manage credentials and login for providers.

```bash
klautcode auth [command]
```

---

#### login

Klautcode is powered by the provider list at [Models.dev](https://models.dev), so you can use `klautcode auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/klautcode/auth.json`.

```bash
klautcode auth login
```

When Klautcode starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

##### Flags

| Flag                                     | Short | Description                                          |
| ---------------------------------------- | ----- | ---------------------------------------------------- |
| <nobr><code>{"--provider"}</code></nobr> | `-p`  | Provider ID or name to log in to                     |
| <nobr><code>{"--method"}</code></nobr>   | `-m`  | Login method label to use, skipping method selection |

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
klautcode auth list
```

Or the short version.

```bash
klautcode auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
klautcode auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
klautcode github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
klautcode github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
klautcode github run
```

##### Flags

| Flag                                  | Description                            |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event to run the agent for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
klautcode mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
klautcode mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
klautcode mcp list
```

Or use the short version.

```bash
klautcode mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
klautcode mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
klautcode mcp auth list
```

Or use the short version.

```bash
klautcode mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
klautcode mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
klautcode mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
klautcode models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
klautcode models anthropic
```

#### Flags

| Flag                                    | Description                                                  |
| --------------------------------------- | ------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Refresh the models cache from models.dev                     |
| <nobr><code>{"--verbose"}</code></nobr> | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Klautcode.

```bash
klautcode models --refresh
```

---

### run

Run klautcode in non-interactive mode by passing a prompt directly.

```bash
klautcode run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "klautcode run"
klautcode run Explain the use of context in Go
```

You can also attach to a running `klautcode serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
klautcode serve

# In another terminal, run commands that attach to it
klautcode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | The command to run, use message for args                                   |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--share"}</code></nobr>    |       | Share the session                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Model to use in the form of provider/model                                 |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agent to use                                                               |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | File(s) to attach to message                                               |
| <nobr><code>{"--format"}</code></nobr>   |       | Format: default (formatted) or json (raw JSON events)                      |
| <nobr><code>{"--title"}</code></nobr>    |       | Title for the session (uses truncated prompt if no value provided)         |
| <nobr><code>{"--attach"}</code></nobr>   |       | Attach to a running klautcode server (e.g., http://localhost:4096)          |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `KLAUTCODE_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `KLAUTCODE_SERVER_USERNAME` or `klautcode`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directory to run in, or path on the remote server when attaching           |
| <nobr><code>{"--port"}</code></nobr>     |       | Port for the local server (defaults to random port)                        |
| <nobr><code>{"--variant"}</code></nobr>  |       | Model variant (provider-specific reasoning effort)                         |
| <nobr><code>{"--thinking"}</code></nobr> |       | Show thinking blocks                                                       |
| <nobr><code>{"--auto"}</code></nobr>     |       | Auto-approve permissions that are not explicitly denied                    |

---

### serve

Start a headless Klautcode server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
klautcode serve
```

This starts an HTTP server that provides API access to klautcode functionality without the TUI interface. Set `KLAUTCODE_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `klautcode`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### session

Manage Klautcode sessions.

```bash
klautcode session [command]
```

---

#### list

List all Klautcode sessions.

```bash
klautcode session list
```

##### Flags

| Flag                                      | Short | Description                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limit to N most recent sessions      |
| <nobr><code>{"--format"}</code></nobr>    |       | Output format: table or json (table) |

---

#### delete

Delete an Klautcode session.

```bash
klautcode session delete <sessionID>
```

---

### stats

Show token usage and cost statistics for your Klautcode sessions.

```bash
klautcode stats
```

#### Flags

| Flag                                    | Description                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Show stats for the last N days (all time)                                   |
| <nobr><code>{"--tools"}</code></nobr>   | Number of tools to show (all)                                               |
| <nobr><code>{"--models"}</code></nobr>  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| <nobr><code>{"--project"}</code></nobr> | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
klautcode export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

#### Flags

| Flag                                     | Description                           |
| ---------------------------------------- | ------------------------------------- |
| <nobr><code>{"--sanitize"}</code></nobr> | Redact sensitive transcript/file data |

---

### import

Import session data from a JSON file or Klautcode share URL.

```bash
klautcode import <file>
```

You can import from a local file or an Klautcode share URL.

```bash
klautcode import session.json
klautcode import https://opncd.ai/s/abc123
```

---

### web

Start a headless Klautcode server with a web interface.

```bash
klautcode web
```

This starts an HTTP server and opens a web browser to access Klautcode through a web interface. Set `KLAUTCODE_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `klautcode`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
klautcode acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr>         | Working directory                          |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### plugin

Install a plugin and update your config.

```bash
klautcode plugin <module>
```

Or use the alias.

```bash
klautcode plug <module>
```

#### Flags

| Flag                                   | Short | Description                     |
| -------------------------------------- | ----- | ------------------------------- |
| <nobr><code>{"--global"}</code></nobr> | `-g`  | Install in global config        |
| <nobr><code>{"--force"}</code></nobr>  | `-f`  | Replace existing plugin version |

---

### pr

Fetch and checkout a GitHub PR branch, then run Klautcode.

```bash
klautcode pr <number>
```

---

### db

Database tools.

```bash
klautcode db [query]
```

#### Flags

| Flag                                   | Description                    |
| -------------------------------------- | ------------------------------ |
| <nobr><code>{"--format"}</code></nobr> | Output format: `json` or `tsv` |

---

#### path

Print the database path.

```bash
klautcode db path
```

---

### debug

Debugging and troubleshooting tools.

```bash
klautcode debug [command]
```

---

### uninstall

Uninstall Klautcode and remove all related files.

```bash
klautcode uninstall
```

#### Flags

| Flag                                        | Short | Description                                 |
| ------------------------------------------- | ----- | ------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Keep configuration files                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Keep session data and snapshots             |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Show what would be removed without removing |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates klautcode to the latest version or a specific version.

```bash
klautcode upgrade [target]
```

To upgrade to the latest version.

```bash
klautcode upgrade
```

To upgrade to a specific version.

```bash
klautcode upgrade v0.1.48
```

#### Flags

| Flag                                   | Short | Description                                                       |
| -------------------------------------- | ----- | ----------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The klautcode CLI takes the following global flags.

| Flag                                       | Short | Description                          |
| ------------------------------------------ | ----- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Display help                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Print version number                 |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Print logs to stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Log level (DEBUG, INFO, WARN, ERROR) |
| <nobr><code>{"--pure"}</code></nobr>       |       | Run without external plugins         |

---

## Environment variables

Klautcode can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `KLAUTCODE_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `KLAUTCODE_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `KLAUTCODE_CONFIG`                     | string  | Path to config file                               |
| `KLAUTCODE_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `KLAUTCODE_CONFIG_DIR`                 | string  | Path to config directory                          |
| `KLAUTCODE_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `KLAUTCODE_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `KLAUTCODE_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `KLAUTCODE_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `KLAUTCODE_PERMISSION`                 | string  | Inlined json permissions config                   |
| `KLAUTCODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `KLAUTCODE_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `KLAUTCODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `KLAUTCODE_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `KLAUTCODE_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `KLAUTCODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `KLAUTCODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `KLAUTCODE_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `KLAUTCODE_DISABLE_MOUSE`              | boolean | Disable mouse capture in the TUI                  |
| `KLAUTCODE_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `KLAUTCODE_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `KLAUTCODE_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `KLAUTCODE_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `KLAUTCODE_SERVER_USERNAME`            | string  | Override basic auth username (default `klautcode`) |
| `KLAUTCODE_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `KLAUTCODE_EXPERIMENTAL`                         | boolean | Enable the experimental umbrella flag   |
| `KLAUTCODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `KLAUTCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `KLAUTCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `KLAUTCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `KLAUTCODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `KLAUTCODE_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `KLAUTCODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `KLAUTCODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `KLAUTCODE_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `KLAUTCODE_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `KLAUTCODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
| `KLAUTCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Enable background subagent tasks        |
| `KLAUTCODE_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Enable experimental event system        |
| `KLAUTCODE_EXPERIMENTAL_NATIVE_LLM`              | boolean | Enable native LLM request path          |
| `KLAUTCODE_EXPERIMENTAL_PARALLEL`                | boolean | Enable parallel web search execution    |
| `KLAUTCODE_EXPERIMENTAL_SCOUT`                   | boolean | Enable Scout subagent                   |
| `KLAUTCODE_EXPERIMENTAL_WORKSPACES`              | boolean | Enable workspace support                |
