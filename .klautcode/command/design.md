---
description: "enter design mode to preview and edit a UI"
---

Enter design mode. Follow the **design** skill.

Target for this session: $ARGUMENTS

1. Identify the page, route, or component to design against.
2. Preview it (browser automation or webfetch) and inspect the current design.
3. Map visual issues back to source files (component/CSS).
4. Propose then apply targeted edits, keeping them consistent with the design system.
5. Re-preview and verify the change.

Use the **goal** skill for multi-step design tasks, and the **self-improve** skill if a reusable lesson surfaces. State the target and first step before editing.
